require 'rails_helper'

RSpec.describe 'Rack::Attack', type: :request do
  # TODO: Create rack attack tests
  # context 'when rate limiting' do
  #   it 'throttles requests after limit is reached' do
  #     3.times { post '/api/vote' }
  #     expect(response.status).to eq(429)
  #   end
  # end

  # context 'when blocking bad user agents' do
  #   it 'blocks requests from bad user agent' do
  #     post '/api/vote', headers: { 'User-Agent' => 'BadBot' }
  #     expect(response.status).to eq(403)
  #   end
  # end
end
